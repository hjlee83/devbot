name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read
  issues: read
  pull-requests: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Install uv
        uses: astral-sh/setup-uv@v8.3.2
        with:
          python-version: "3.13"

      - name: uv sync
        run: uv sync

      - name: uv run ruff check .
        run: uv run ruff check .

      - name: uv run pytest
        run: uv run pytest

      - name: uv run devbot --once --dry-run
        run: uv run devbot --once --dry-run
        env:
          # No .env is committed; devbot requires WORKSPACE_ROOT and
          # GITHUB_TOKEN to start. config/repositories.yaml ships its
          # sample repository disabled, so no repository path needs to
          # actually exist and no real GitHub API call is ever made.
          # --once is required here: bare `uv run devbot` runs the
          # continuous polling loop and would never return. --dry-run
          # guarantees no write/commit/push/PR path runs here regardless
          # of DRY_RUN, matching Task 009's smoke verification command.
          WORKSPACE_ROOT: ${{ runner.temp }}
          GITHUB_TOKEN: ${{ github.token }}
